def soma2(v1:float, v2:float):
    """a função soma2, realiza a soma entre dois valores 
    Args: 
        v1(float): Passe um numero inteiro ou decimal
        v2(float): Passe um numero inteiro ou decimal
    Returns:
        float: Retorna o resultado da soma entre dois valores     
    """
    return v1 + v2

def resto(v1:int , v2: int):
    """A função resto retorna o resta da divisão entre doi valores inteiros
    Args:
        v1(int) Passe um numero inteiro que sera o dividendo
        v2(int) Passe um numero inteiro que sera o divisor
    Returns:
        int: Retorna o resto da divisão entre os números """
    return v1 % v2

def retornaPorcentagem(v1,float):
    """A função retorna porcentagem, recebe um valor decimal e retorna
    o mesmo valor em porcentagemn Por exemplo: você passa o valor 6 ou 6.5
    e ele retornará 0.06(6%) e 0.65(6.5%) respectivamente
    Args:     """

    